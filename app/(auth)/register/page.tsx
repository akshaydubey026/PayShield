export const metadata = {
  title: "Register | PayShield",
  description: "Create your PayShield account",
};

/** Route exists for metadata + URL; UI is rendered by `(auth)/layout` → {@link SplitAuthLayout}. */
export default function RegisterPage() {
  return null;
}
